import { useState } from 'react';
import employees from '../data/employees.json';
import styles from './Pages.module.css';

export function Employees() { const [query, setQuery] = useState(''); const filtered = employees.filter((employee) => Object.values(employee).some((value) => value.toLowerCase().includes(query.toLowerCase()))); return <div className={styles.page}><div className={styles.pageIntro}><div><p className={styles.eyebrow}>Our people</p><h2>Employee directory</h2><p className={styles.subtitle}>Find a colleague, team, or department.</p></div><span className={styles.directoryCount}>{employees.length} people</span></div><div className={styles.tableWrap}><div className={styles.directoryToolbar}><div className={styles.searchBox}><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by name, role, or team" aria-label="Search employees" /></div><button className={styles.filterButton}>Filter <span>⌄</span></button></div><div className={styles.tableScroll}><table><thead><tr><th>Name</th><th>Role</th><th>Department</th><th>Email</th><th></th></tr></thead><tbody>{filtered.map((employee) => <tr key={employee.email}><td><div className={styles.personCell}><div className={styles.smallAvatar}>{employee.name.split(' ').map((part) => part[0]).join('').slice(0, 2)}</div><strong>{employee.name}</strong></div></td><td>{employee.role}</td><td><span className={styles.department}>{employee.department}</span></td><td><a className={styles.email} href={`mailto:${employee.email}`}>{employee.email}</a></td><td className={styles.rowArrow}>↗</td></tr>)}</tbody></table>{filtered.length === 0 && <div className={styles.empty}>No employees match that search.</div>}</div></div></div>; }
