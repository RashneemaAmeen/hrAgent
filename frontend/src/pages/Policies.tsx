import { useState } from 'react';
import policies from '../data/policies.json';
import styles from './Pages.module.css';

export function Policies() { const [open, setOpen] = useState(0); return <div className={styles.page}><div className={styles.pageIntro}><div><p className={styles.eyebrow}>The PeopleHub handbook</p><h2>HR policies</h2><p className={styles.subtitle}>Clear answers to the questions that shape your workday.</p></div><span className={styles.directoryCount}>{policies.length} policies</span></div><div className={styles.policyList}>{policies.map((policy, index) => <article className={`${styles.policyItem} ${open === index ? styles.policyOpen : ''}`} key={policy.title}><button className={styles.policyButton} onClick={() => setOpen(open === index ? -1 : index)} aria-expanded={open === index}><span className={styles.policyNumber}>{String(index + 1).padStart(2, '0')}</span><span><strong>{policy.title}</strong><small>{policy.summary}</small></span><span className={styles.plus}>{open === index ? '−' : '+'}</span></button>{open === index && <div className={styles.policyBody}><p>{policy.body}</p><button className={styles.textButton}>Open full policy <span>↗</span></button></div>}</article>)}</div></div>; }
